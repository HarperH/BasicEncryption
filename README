This is a basic encryption program using a password and keyfile

Make sure that the keyfile is downloaded and kept in the same folder that the program is being run from
Only works on .txt documents
Easiest if .txt docs are in same folder, otherwise be sure to specify path from current folder.
